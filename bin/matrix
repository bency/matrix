#!/usr/bin/env php
<?php
include __DIR__ . "/../vendor/autoload.php";

$debug = false;
foreach ($_SERVER['argv'] as $arg) {
    if ('--debug' == $arg) {
        $debug = true;
    }
}
$layout = new Matrix\Layout();
$layout->setWording('MATRIX RAIN');
$layout->enableMarquee();
$layout->run($debug);
